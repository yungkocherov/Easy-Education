/* ==========================================================================
   Глоссарий: SVD (Singular Value Decomposition)
   ========================================================================== */
App.registerTopic({
  id: 'glossary-svd',
  category: 'glossary',
  title: 'SVD — сингулярное разложение',
  summary: 'Разложение любой матрицы на три: вращение → масштабирование → вращение. Основа PCA, рекомендательных систем, сжатия данных.',

  tabs: {
    theory: `
      <div class="intuition">
        <div class="intuition-title">Аналогия</div>
        <p>SVD — это «рентген» для матрицы. Оно говорит: любая матрица $X$ может быть представлена как <b>три простые операции</b>: повернуть пространство, растянуть оси, снова повернуть. Эти три шага полностью описывают, что делает матрица.</p>
        <p>SVD работает для <b>любой</b> матрицы (прямоугольной, не обратимой, с комплексными значениями). В этом его отличие от разложения через собственные векторы, которое работает только для квадратных.</p>
      </div>

      <h3>📐 Определение</h3>
      <div class="math-block">$$X = U \\Sigma V^T$$</div>
      <p>где для матрицы $X$ размером $m \\times n$:</p>
      <ul>
        <li>$U$ — ортогональная матрица $m \\times m$ (столбцы — левые сингулярные векторы).</li>
        <li>$\\Sigma$ — «диагональная» матрица $m \\times n$ с <b>сингулярными значениями</b> $\\sigma_1 \\geq \\sigma_2 \\geq \\ldots \\geq 0$ на диагонали.</li>
        <li>$V^T$ — транспонированная ортогональная матрица $n \\times n$ (строки — правые сингулярные векторы).</li>
      </ul>
      <p>«Ортогональная» означает $U^T U = I$ — повороты/отражения, сохраняющие длины.</p>

      <h3>🎯 Связь с собственными значениями</h3>
      <ul>
        <li>$V$ — собственные векторы $X^TX$ (ковариационная матрица столбцов).</li>
        <li>$U$ — собственные векторы $XX^T$.</li>
        <li>$\\sigma_i^2$ — собственные значения $X^TX$ (и $XX^T$).</li>
      </ul>
      <p>Для симметричной положительной матрицы SVD и eigen-разложение совпадают (с точностью до знаков).</p>

      <h3>📊 Геометрический смысл</h3>
      <p>Умножение $Xv$ (где $v$ — единичный вектор) эквивалентно:</p>
      <ol>
        <li>Разложить $v$ в базисе $V$: $V^T v$ (поворот в новый базис).</li>
        <li>Растянуть координаты на $\\sigma_i$: $\\Sigma V^T v$.</li>
        <li>Повернуть в базис $U$: $U \\Sigma V^T v$.</li>
      </ol>
      <p>Единичная сфера после $X$ превращается в <b>эллипсоид</b>, оси которого — столбцы $U$, а полуоси — $\\sigma_i$.</p>

      <h3>🔢 Truncated SVD (обрезанное разложение)</h3>
      <p>Если оставить только первые $k$ сингулярных значений (остальные занулить):</p>
      <div class="math-block">$$X_k = U_k \\Sigma_k V_k^T$$</div>
      <p>$X_k$ — <b>наилучшее</b> приближение $X$ матрицей ранга $k$ (по норме Фробениуса). Это теорема Эккарта-Янга.</p>
      <p>Применения truncated SVD:</p>
      <ul>
        <li><b>Сжатие изображений</b>: 1000×1000 матрицу можно заменить на $U_k$ (1000×50) + σ (50) + $V_k^T$ (50×1000) — 100K чисел вместо 1M, визуально почти идентично.</li>
        <li><b>Denoising</b>: шум часто лежит в малых сингулярных значениях; обрезав хвост, получим более чистый сигнал.</li>
        <li><b>LSA/LSI</b> (Latent Semantic Analysis): truncated SVD матрицы «документы × слова» выделяет скрытые темы.</li>
        <li><b>Matrix Factorization</b> в recsys: $R \\approx U_k \\Sigma_k V_k^T$ — юзеры и айтемы в латентном пространстве.</li>
      </ul>

      <h3>💡 Связь с PCA</h3>
      <p>SVD — численно устойчивый способ сделать PCA. Вместо вычисления $\\Sigma = X^TX$ и поиска её собственных значений (плохая обусловленность), делают SVD самой $X$:</p>
      <div class="math-block">$$X = U \\Sigma V^T \\Rightarrow X^TX = V \\Sigma^2 V^T$$</div>
      <p>Главные компоненты = столбцы $V$. Дисперсии по компонентам = $\\sigma_i^2 / (n-1)$.</p>
      <p>sklearn <code>PCA</code> внутри использует именно SVD, а не ручное собственное разложение.</p>

      <h3>🎨 Пример сжатия изображения</h3>
      <p>У изображения 500×500 (= 250,000 пикселей, одноканального).</p>
      <div class="calc">SVD: X = U (500×500) × Σ (500) × Vᵀ (500×500)

Полное хранение U, Σ, V = 500² + 500 + 500² ≈ 500K чисел (хуже!)

Но truncated SVD с k=50:
  U_50 (500×50) + Σ_50 (50) + V_50ᵀ (50×500)
  = 25,000 + 50 + 25,000 ≈ 50K чисел (<b>5× сжатие</b>)

Качество: если σ_51, σ_52, ... малы (типично для фото) — визуально почти без потерь.</div>

      <h3>🔢 Алгоритмы вычисления</h3>
      <ul>
        <li><b>Jacobi SVD</b>, <b>Golub-Kahan-Reinsch</b> — классические $O(mn^2)$ методы для точной SVD.</li>
        <li><b>Randomized SVD</b> — приближённая $O(mn \\log k)$ для truncated SVD, работает на гигантских матрицах.</li>
        <li><b>Incremental SVD</b> — для потоковых данных.</li>
      </ul>

      <h3>🔗 Связанные темы</h3>
      <ul>
        <li><a onclick="App.selectTopic('pca')">PCA</a> — прямое применение SVD</li>
        <li><a onclick="App.selectTopic('glossary-eigen')">Собственные векторы</a> — родственное понятие</li>
        <li><a onclick="App.selectTopic('recsys')">Рекомендательные системы</a> — matrix factorization через SVD</li>
        <li><a onclick="App.selectTopic('glossary-matrix-factorization')">Матричная факторизация</a></li>
      </ul>
    `,

    links: `
      <h3>📖 Ресурсы</h3>
      <ul>
        <li><a href="https://en.wikipedia.org/wiki/Singular_value_decomposition" target="_blank">Wikipedia: SVD</a></li>
        <li><a href="https://www.youtube.com/watch?v=EokL7E6o1AE" target="_blank">3Blue1Brown: SVD intuition</a></li>
      </ul>
    `
  }
});
