/* ==========================================================================
   Глоссарий: Bootstrap и OOB (Out-of-Bag)
   ========================================================================== */
App.registerTopic({
  id: 'glossary-bootstrap',
  category: 'glossary',
  title: 'Bootstrap и OOB',
  summary: 'Выборка с возвращением — универсальный способ оценить разброс статистики без теории. Основа Random Forest и непараметрических CI.',

  tabs: {
    theory: `
      <div class="intuition">
        <div class="intuition-title">Аналогия</div>
        <p>У тебя 100 клиентов и их доходы. Ты хочешь оценить среднее и понять, насколько точна эта оценка. Но второй выборки нет — и у тебя только эти 100 наблюдений.</p>
        <p><b>Bootstrap</b> — элегантный трюк: «воспри́ми выборку как саму популяцию». Из исходных 100 клиентов делай новые выборки размера 100 <b>с возвращением</b> (то есть один клиент может попасть в выборку 2 раза, другой — 0 раз). Каждая такая «псевдо-выборка» даёт свою оценку среднего. Повторив 1000 раз, получишь распределение средних — и сможешь построить CI, не предполагая никакого распределения.</p>
      </div>

      <h3>📐 Алгоритм</h3>
      <ol>
        <li>Есть выборка $D = \\{x_1, \\ldots, x_n\\}$.</li>
        <li>Сделай $B$ ресемплов (обычно $B = 1000$-$10000$). Каждый ресемпл $D^*_b$ — случайно выбранные $n$ элементов <b>с возвращением</b>.</li>
        <li>Для каждого ресемпла посчитай статистику $T_b = f(D^*_b)$ (например, среднее, медиану, Cohen's d).</li>
        <li>Распределение $\\{T_1, \\ldots, T_B\\}$ — приближение распределения статистики.</li>
        <li>95% CI: квантили 2.5% и 97.5% этого распределения (percentile bootstrap).</li>
      </ol>

      <h3>🔢 Пример: CI для медианы</h3>
      <div class="calc">Данные: [3, 7, 2, 8, 5, 9, 1, 6, 4, 10]
Медиана исходной: 5.5

Ресемпл 1: [3, 3, 8, 5, 9, 6, 4, 10, 1, 7] → медиана 5.5
Ресемпл 2: [7, 7, 2, 2, 5, 9, 1, 6, 4, 10] → медиана 5.5
Ресемпл 3: [3, 8, 5, 9, 1, 1, 6, 4, 10, 10] → медиана 5.5
...
(повторим 1000 раз)

95% CI = [2.5-й квантиль, 97.5-й квантиль] медиан
Для медианы у нас нет простой формулы CI (как для среднего),
bootstrap — лучший способ.</div>

      <h3>💡 Почему работает</h3>
      <p>Ключевое предположение: выборка «репрезентативна» для популяции. Тогда распределение $T(D^*)$ приближает распределение $T(D)$ — то есть разброс статистики, если бы мы делали новые выборки из реальной популяции. Это называется <b>«plug-in» принципом</b>.</p>
      <p>Bootstrap работает для <b>почти любых статистик</b>: среднее, медиана, дисперсия, коэффициент регрессии, корреляция, min, max, квантиль. Главное, чтобы статистика была достаточно гладкой функцией данных.</p>

      <h3>⚠️ Когда НЕ работает</h3>
      <ul>
        <li><b>Крайние статистики</b> (min, max). Они определяются одной точкой, и при ресемплинге очень нестабильны.</li>
        <li><b>Тяжёлые хвосты</b> (например, доходы у миллиардеров). Bootstrap недооценивает неопределённость, если в выборке нет «экстремумов».</li>
        <li><b>Зависимые данные</b> (временные ряды, сети). Обычный iid-бутстрэп ломает зависимость. Нужен <b>block bootstrap</b> (ресемплируются блоки).</li>
        <li><b>Маленькие выборки</b> ($n < 20$). Результат слишком оптимистичен.</li>
      </ul>

      <h3>🎯 Bootstrap в машинном обучении: Bagging</h3>
      <p><b>Bagging</b> (Bootstrap Aggregating) — идея ансамбля: обучить $B$ моделей на $B$ bootstrap-выборках и усреднить их предсказания. Это уменьшает дисперсию модели.</p>
      <p>Именно на этом построен <a onclick="App.selectTopic('random-forest')">Random Forest</a>: каждое дерево обучается на своём bootstrap-сэмпле.</p>

      <h3>📊 OOB — Out-of-Bag</h3>
      <p>При bootstrap с $n$ элементов, вероятность, что конкретный элемент $x_i$ <b>не попадёт</b> в ресемпл, равна:</p>
      <div class="math-block">$$P(\\text{x_i не в ресемпле}) = \\left(1 - \\frac{1}{n}\\right)^n \\xrightarrow{n \\to \\infty} e^{-1} \\approx 0.368$$</div>
      <p>Значит, примерно <b>37% данных</b> не попадают в каждый конкретный bootstrap. Эти 37% называются <b>out-of-bag (OOB)</b> для этого ресемпла.</p>

      <h3>💡 OOB-оценка качества</h3>
      <p>В Random Forest каждое дерево не видит ~37% данных. Можно использовать эти точки как «бесплатный test set»:</p>
      <ol>
        <li>Для каждой точки $x_i$ найди все деревья, где $x_i$ была OOB.</li>
        <li>Предскажи $x_i$ только этими деревьями (усредняй их голоса).</li>
        <li>Сравни с истинной меткой $y_i$.</li>
      </ol>
      <p>OOB score — почти эквивалент cross-validation, но <b>бесплатный</b>: не нужно делать отдельную разбивку. В sklearn <code>RandomForestClassifier(oob_score=True)</code>.</p>

      <h3>📈 Типы bootstrap CI</h3>
      <ul>
        <li><b>Percentile method</b>: просто квантили $T^*$. Самый простой.</li>
        <li><b>Basic bootstrap</b>: $2T - T^*_{97.5}$, $2T - T^*_{2.5}$. Учитывает смещение.</li>
        <li><b>BCa (Bias-Corrected and accelerated)</b>: поправка на смещение и асимметрию. Лучший по точности.</li>
        <li><b>Studentized</b>: t-образная ресемпл-статистика $(T^* - T)/\\text{SE}^*$. Самый точный, но дорогой.</li>
      </ul>

      <h3>🔗 Связанные темы</h3>
      <ul>
        <li><a onclick="App.selectTopic('random-forest')">Random Forest</a> — bagging в действии</li>
        <li><a onclick="App.selectTopic('glossary-confidence-interval')">Доверительный интервал</a></li>
        <li><a onclick="App.selectTopic('cross-validation')">Cross-validation</a> — альтернатива OOB</li>
        <li><a onclick="App.selectTopic('bias-variance')">Bias-Variance</a> — bagging уменьшает variance</li>
      </ul>
    `,

    links: `
      <h3>📖 Ресурсы</h3>
      <ul>
        <li><a href="https://en.wikipedia.org/wiki/Bootstrapping_(statistics)" target="_blank">Wikipedia: Bootstrap</a></li>
        <li><a href="https://en.wikipedia.org/wiki/Out-of-bag_error" target="_blank">Out-of-bag error</a></li>
      </ul>
    `
  }
});
